/**
 * 레지스트리 표준 키 `image`용 컴포넌트.
 * 호환성 래퍼(compatibility wrapper): 구현은 `ImageMediaIcon`을 그대로 re-export 합니다.
 * 별도 SVG가 생기면 이 파일에서 전환하면 됩니다.
 */
export { ImageMediaIcon as ImageIcon } from "@/icons/system/ImageMediaIcon";
