/// <reference types="vitest/globals" />

/**
 * Vitest globals for `tsc --noEmit` (vitest.config sets globals: true at runtime).
 * Keep production ProcessEnv / contracts unchanged — this file is types-only.
 */
export {};
